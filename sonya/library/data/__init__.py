"""Sonya library data assets — packaged JSON content for the runtime.

Files in this directory are loaded via `importlib.resources` from
`sonya.library.loader`. They are content (not code) — edit via PR.
"""

# Sonya — runtime + dev dependencies (pinned).
# Установка: pip install -r requirements.txt
#
# Этот файл — для людей, которым не хочется возиться с pyproject.toml.
# Под капотом тот же набор пакетов, но с зафиксированными версиями.
# Если меняешь зависимости — правь pyproject.toml И этот файл оба.

# --- Telegram userbot ---
Telethon==1.43.2
pyaes==1.6.1
pyasn1==0.6.3
rsa==4.9.1

# --- LLM (OpenAI-compatible: OpenRouter, NVIDIA, Groq, DeepSeek, etc.) ---
openai==1.109.0
distro==1.9.0
docstring_parser==0.18.0
jiter==0.14.0

# --- LLM (Google Gemini native SDK) ---
google-genai==1.73.1
google-auth==2.49.2
cryptography==47.0.0
pyasn1_modules==0.4.2
requests==2.33.1
charset-normalizer==3.4.7
urllib3==2.6.3
websockets==16.0
pycparser==3.0

# --- HTTP / async ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
sniffio==1.3.1
certifi==2026.4.22
idna==3.13

# --- Config ---
pydantic==2.13.3
pydantic-settings==2.14.0
pydantic_core==2.46.3
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0
python-dotenv==1.2.2

# --- Database ---
SQLAlchemy==2.0.49
aiosqlite==0.22.1
alembic==1.18.4
Mako==1.3.11
MarkupSafe==3.0.3
greenlet==3.4.0

# --- Scheduler ---
APScheduler==3.11.2
tzlocal==5.3.1

# --- Logging ---
loguru==0.7.3

# --- Utils ---
tenacity==9.1.4
librt==0.9.0
packaging==26.2
Pygments==2.20.0

# --- Dev tools (тесты, линтер, mypy) ---
pytest==9.0.3
pytest-asyncio==1.3.0
iniconfig==2.3.0
pluggy==1.6.0
ruff==0.15.12
mypy==1.20.2
mypy_extensions==1.1.0
pathspec==1.1.0
